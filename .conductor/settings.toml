"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "cd web && npm install"
run_mode = "concurrent"

[scripts.run.dev]
available_in = [ "local" ]
command = "cd web && npm run dev -- --port $CONDUCTOR_PORT"
default = true
icon = "play"
